{
  "name": "aibos-metadata-monorepo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "turbo run start",
    "lint": "turbo run lint",
    "clean": "turbo run clean",
    "sync-mcp-prompt": "tsx scripts/sync-mcp-prompt.ts",
    "lint:ui-constitution": "tsx scripts/validate-ui-constitution.ts",
    "generate:ui": "tsx scripts/generate-ui-component.ts"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "tsx": "^4.19.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@8.15.0"
}
