/**
 * Popover Component - Layer 2 Radix Composition
 *
 * Barrel export for Popover component and all its parts
 *
 * @module Popover
 * @layer 2
 */

export {
  Popover,
  PopoverAnchor,
  PopoverClose,
  PopoverContent,
  PopoverTrigger,
} from './popover'

export type {
  PopoverArrowProps,
  PopoverCloseProps,
  PopoverContentProps,
  PopoverRootProps,
  PopoverSize,
  PopoverTriggerProps,
  PopoverVariant,
} from './popover.types'

export { default } from './popover'
